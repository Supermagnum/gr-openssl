id: crypto_sym_enc
label: Symmetric Encryption
category: '[OpenSSL]'
flags: [ python ]

parameters:
-   id: cipher
    label: cipher definition
    dtype: raw

inputs:
-   domain: message
    id: pdus
    optional: true

outputs:
-   domain: message
    id: pdus
    optional: true

templates:
    imports: import crypto
    make: crypto.sym_enc(${cipher})

file_format: 1
